<%= form_for(@loan_transaction) do |f| %>
  <% if @loan_transaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@loan_transaction.errors.count, "error") %> prohibited this loan_transaction from being saved:</h2>

      <ul>
      <% @loan_transaction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :donner_id %><br />
    <%= f.select :donner_id,options_for_select(Donner.all.collect { |d| [d.first_name + " " + d.last_name , d.id] }) %>
  </div>
  <div class="field">
    <%= f.label :applicant_id %><br />
    <%= f.select :applicant_id,options_for_select(Applicant.all.collect { |a| [a.first_name + " " + a.last_name , a.id] }) %>
  </div>
  <div class="field">
    <%= f.label :debit, "Loan taken" %><br />
    <%= f.text_field :debit %>
  </div>
  <div class="field">
    <%= f.label :credit, "Amount returned" %><br />
    <%= f.text_field :credit %>
  </div>
  <div class="field">
    <%= f.label :comment %><br />
    <%= f.text_field :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
